import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cryptosentinelv2.app',
  appName: 'CryptoSentinelV2 AI',
  webDir: 'dist',
  android: {
    backgroundColor: '#0a0e1a'
  }
};

export default config;
